import 'package:flutter/material.dart';

class ReaderMenuMain extends StatelessWidget {
  const ReaderMenuMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
